import React from 'react';
import styled from '@emotion/styled';
import { bodyFont } from '../../styles/mixin';

export default function CharactersList() {
  return <Wrapper>CharactersList</Wrapper>;
}

const Wrapper = styled.header`
  width: 100%;
  flex: 1;
  ${bodyFont};
`;
